{
  "name": "lld-practice",
  "private": true,
  "scripts": {
    "vercel-build": "npm run build --prefix web && rm -rf public && mkdir -p public && cp -r web/dist/. public/ && npm run prisma:generate --prefix server && npm run build --prefix server"
  }
}
