{
  "name": "lld-practice",
  "private": true,
  "scripts": {
    "vercel-build": "npm run build --prefix web && rm -rf public && mkdir -p public && cp -r web/dist/. public/ && npx --yes esbuild server/src/vercel-handler.ts --bundle --platform=node --format=cjs --outfile=\"api/[...path].js\" --footer:js=\"module.exports = module.exports.default;\""
  }
}
