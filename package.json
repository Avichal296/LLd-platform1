{
  "name": "lld-practice",
  "private": true,
  "scripts": {
    "vercel-build": "npm run build --prefix web && rm -rf public && mkdir -p public && cp -r web/dist/. public/ && npx prisma generate --schema=server/prisma/schema.prisma"
  }
}